# MC_PING
minimal Minecraft protocol client written in only one C file. Connects to a Minecraft server, performs the handshake and sends a status request. I made it as a component of a bigger project.
